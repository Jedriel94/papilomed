# Configuración con credenciales reales (NO subir)
api/config.php

# Archivos de guías subidos por los usuarios (se conserva solo la protección)
uploads/*
!uploads/.htaccess

# Sistema
.DS_Store

# Node (versión anterior; ya no se usa, por si quedan restos)
node_modules/
.env
