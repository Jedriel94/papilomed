# Reenvía cualquier petición de /api/* al front controller index.php,
# salvo que el archivo exista físicamente (p. ej. setup.php).
RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
